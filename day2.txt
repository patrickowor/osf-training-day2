things we learnt:

git init // we initialized a project
git commit // we commited a project
git add --all (staging) // we staged a project and we made a stage of the local changes as short term memory storage
git push // we pushed it to the remote
